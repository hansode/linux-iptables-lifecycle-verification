TARGETS=basic-lifecycle disabled-module-unload dynamic-rules enabled-save-on-restart

all: $(TARGETS)

basic-lifecycle:
	(cd $@ && make)
disabled-module-unload:
	(cd $@ && make)
dynamic-rules:
	(cd $@ && make)
enabled-save-on-restart:
	(cd $@ && make)

.PHONY: $(TARGETS)
